{
  "name": "@smartpay/api-gateway",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "test": "echo \"No tests yet for @smartpay/api-gateway\"",
    "db:generate": "echo \"No database generation yet for @smartpay/api-gateway\"",
    "db:migrate": "echo \"No database migrations yet for @smartpay/api-gateway\""
  },
  "dependencies": {
    "@smartpay/shared": "workspace:*"
  }
}
