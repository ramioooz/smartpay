describe.skip('integration/reconciliation-batch', () => {
  it('runs reconciliation batch and classifies discrepancies', () => {
    // TODO: seed settled payments and compare against provider snapshots.
  });
});
