describe.skip('integration/payment-flow', () => {
  it('creates merchant, submits payment, and observes settled lifecycle', () => {
    // TODO: execute full flow against dockerized stack and verify emitted Kafka topics.
  });
});
