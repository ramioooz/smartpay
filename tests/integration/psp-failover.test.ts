describe.skip('integration/psp-failover', () => {
  it('fails primary PSP and routes to secondary adapter', () => {
    // TODO: inject deterministic adapter failures and verify fallback routing decision.
  });
});
